# Database

Thu muc nay chua schema va seed data cho MySQL.

- `schema.sql`: tao bang `users`, `products`, `interactions` cho demo.
- `seed_demo_users.sql`: danh cho du lieu demo bo sung neu can.

Docker Compose import `schema.sql` khi MySQL khoi tao volume moi.

